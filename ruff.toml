line-length = 140
target-version = "py312"

[lint]
# Keep one explicit repository-wide contract so Ruff upgrades do not silently
# enable new preview/style rules for release scripts.
select = ["E4", "E7", "E9", "F"]
